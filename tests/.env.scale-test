# Configuration for Scale Test VM Creation
# Copy this to .env and modify as needed

# VM Configuration
VM_COUNT=120
VM_SIZE=Standard_B2ls_v2
VM_PREFIX=scale-test-vm
ADMIN_USERNAME=azureuser
SSH_KEY_PATH=$HOME/.ssh/id_rsa.pub

# Azure Configuration
RESOURCE_GROUP=scale-test-rg
LOCATION=italynorth
VNET_NAME=scale-test-vnet
SUBNET_NAME=default

# Performance Configuration
BATCH_SIZE=10  # Number of VMs to create in parallel per batch

# Optional: Use existing network infrastructure
# RESOURCE_GROUP=existing-rg
# VNET_NAME=existing-vnet
# SUBNET_NAME=existing-subnet